<header id="masthead"
        class="site-header"
        role="banner">
    <div class="header-wrap clearfix">
        <div class="site-branding">
            <h1 class="site-title">
                <a href="{{ path('homepage') }}"
                   title="Theme Preview"
                   rel="home">{{ 'ui.title' | trans }}</a>
            </h1>
            <p class="site-description">{{ 'ui.description' | trans }}</p>
        </div>

        <div class="header-search-icon"></div>
        <form role="search"
              method="get"
              class="search-form"
              action="">
            {% set searchForm = search_form(app.request) %}
            {{ form_row(searchForm) }}
            {#                <label>#}
            {#                    <span class="screen-reader-text">Search for:</span>#}
            {#                    <input type="search"#}
            {#                           class="search-field"#}
            {#                           placeholder="Search …"#}
            {#                           value=""#}
            {#                           name="s">#}
            {#                </label>#}
            <input type="submit"
                   class="search-submit"
                   value="Search">
        </form>

        {{ cms_menu_render() | raw }}
    </div>
</header>
